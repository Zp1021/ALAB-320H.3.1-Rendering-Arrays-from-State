import data from "../data.js";

export default function Score({score, date}) {
   let scores = data.learners.map((learner => 
        console.log(learner.scores)
        
    ))
    return (
        
        <div>
            
        </div>
    )
}

